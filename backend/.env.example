# Backend Environment Variables
# Copy this file to .env and adjust values for your environment:
#   cp .env.example .env
# .env is gitignored; .env.example is committed.

# ── CORS ────────────────────────────────────────────────────────
# Comma-separated list of allowed origins.
# Default (below) is dev-only localhost. Never use "*" with credentials.
# Production: set to your frontend URL, e.g. https://your-app.example.com
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ── Ports ────────────────────────────────────────────────────────
# Backend port (default: 8001)
# PORT=8001

# ── Build mode ──────────────────────────────────────────────────
# Set to "commercial" to disable GPL soundfonts at runtime.
# BUILD_MODE=

# ── ML Configuration (all optional; only needed for ML mode) ────
# Path to fine-tuned checkpoint. {soundfont} is replaced with the style id.
# Default: MLtraining/best_model_{soundfont}.pt (relative to repo root)
# MUSICGEN_CHECKPOINT_PATH=MLtraining/best_model_{soundfont}.pt

# Base MusicGen model name on HuggingFace Hub.
# MUSICGEN_MODEL_NAME=facebook/musicgen-melody

# Generation duration in seconds (float).
# MUSICGEN_DURATION=30.0

# Device: "cuda" (GPU) or "cpu". Auto-detected if unset.
# MUSICGEN_DEVICE=cuda

# Sampling parameters (leave commented for defaults).
# MUSICGEN_TOP_K=250
# MUSICGEN_TOP_P=0.0
# MUSICGEN_TEMPERATURE=1.0
