# Frontend Environment Variables
# Copy this file to .env and adjust values for your environment:
#   cp .env.example .env
# .env is gitignored; .env.example is committed. Vite loads .env automatically.
# Restart the dev server after editing.

# ── Backend API base URL ─────────────────────────────────────────
# Default: http://localhost:8001 (local dev, proxied via vite.config.ts)
# Production: set to your backend URL (no trailing slash)
# VITE_API_BASE=http://localhost:8001

# ── Frontend dev-server port ─────────────────────────────────────
# Default: 3000
# VITE_PORT=3000
