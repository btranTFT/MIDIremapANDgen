# Local Training Requirements
# Install these before running training scripts

# Core ML frameworks
torch>=2.0.0
torchaudio>=2.0.0

# AudioCraft and dependencies
# Note: Install audiocraft from source for best compatibility
# git+https://github.com/facebookresearch/audiocraft.git

# Or install from PyPI (may have version conflicts)
# audiocraft>=1.0.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
av>=11.0.0

# Training utilities
tensorboard>=2.13.0
wandb>=0.15.0  # Optional: for experiment tracking

# AudioCraft dependencies
einops>=0.7.0
hydra-core>=1.3.0
omegaconf>=2.3.0
num2words>=0.5.12
scipy>=1.11.0
transformers>=4.30.0
julius>=0.2.7
sentencepiece>=0.1.99
flashy>=0.0.1
xformers>=0.0.20  # Optional: for memory efficiency

# Audio format support
pretty_midi>=0.2.10
music21>=9.1.0

# Utilities
tqdm>=4.65.0
